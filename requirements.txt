# Core dependencies for RealGoodSplits.
# For NVIDIA GPU (CUDA) acceleration on Windows, install torch from the
# PyTorch index FIRST, then run this file, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

demucs>=4.0.1
torch>=2.1
torchaudio>=2.1
customtkinter>=5.2.2
soundfile>=0.12.1
lameenc>=1.7.0

# Optional: enables drag-and-drop in the GUI.
# tkinterdnd2>=0.4.2
